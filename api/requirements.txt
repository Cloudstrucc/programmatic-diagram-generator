# Core dependencies
anthropic==0.40.0
diagrams==0.23.4
python-dotenv==1.0.0

# Graphviz/Diagrams dependencies
graphviz==0.20.1
Pillow==10.2.0

# For Mermaid/PlantUML subprocess management
# (PlantUML and Mermaid are installed via apt/npm, not pip)